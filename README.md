Very simple program that appends one event (the current time) to a stream, and then reads (and prints) all events in that stream.

```
docker compose up -d

sbt run
```

The EventStoreDB is available at http://localhost:2113.
